spring:
  application:
    name: catalog-service
  profiles:
    active: ${spring.profiles.active:local}

grpc:
  server:
    port: 9091

server:
  port: 8083

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
