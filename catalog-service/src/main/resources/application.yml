spring:
  application:
    name: catalog-service
  profiles:
    active: ${spring.profiles.active:local}

server:
  port: 8083

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
