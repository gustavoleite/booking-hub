spring:
  application:
    name: search-service
  profiles:
    active: ${spring.profiles.active:local}
  graphql:
    graphiql:
      enabled: true
      path: /graphiql
    path: /graphql

server:
  port: 8085
